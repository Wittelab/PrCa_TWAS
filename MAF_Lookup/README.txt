NOTE: Due to its size (673 MB), "HRC.r1-1.GRCh37.wgs.mac5.sites.tab.gz" cannot be distributed via github
      and must be downloaded from the Sanger Institute (e.g. `bash download_HRC_sites.sh`).

Scripts/results for looking up the distribution of minor allele frequencies (in the HRC site list) for modeled variants
