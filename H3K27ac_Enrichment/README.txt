NOTE: `*.db` file must be unzipped (e.g. run `gunzip h3k27ac.db.gz`)
NOTE: Due to its size (673 MB), "HRC.r1-1.GRCh37.wgs.mac5.sites.tab.gz" cannot be distributed via github
      and must be downloaded from the Sanger Institute (e.g. run `bash download_HRC_sites.sh`).

Scripts/inputs/outputs for evaluating the enrichment of a set of input variants/regions,
  relative to H3K27ac peaks (.bed files, downloaded from GEO (GSE96652)

